
<!--p8-3(헤더와 푸터가 추가됨에 따라 index 파일이 수정됨)-->
{{>layout/header}}
<!--경로 설정을 해준다. 본 파일이 존재하는 디렉토리는 생략하고 그 뒤부터 써준다-->
<h1>스프링 부트로 시작하는 웹 서비스 Ver.2</h1>
<div class="col-md-12">
    <!--    div는 화면에 얼마만큼 차지를 할 것인가를 뜻함.
       col:column, md:middle사이즈(모니터기준. 992px 이상) 12:가로폭을 12개로 쪼개 쓸것이다.
       컬럼:열(세로) 로우:행(가로)-->

    <div class="row">
        <div class="col-md-6">
<!--            <div class="col-md-6">를 추가하니까 글 등록 버튼과 구글로그인 버튼이 붙어있던게 한칸 떨어져서 나옴-->

    </div>
<!--    p13-2 23:13 구글 로그인 버튼 추가-->

    </div>

<br>
<!--br : 한줄 땔떼 쓰는 테그-->
<table class="table table-horizontal table-bordered">
    <thead class="thead-strong">
    <tr>
<!--        tr: 한 행을 넣어줌-->
        <th>번호</th>
<!--        th : 제목, 머릿글-->
        <th>작성자</th>
        <th>내용</th>
        <th>수정</th>
    </tr>
    </thead>

    <tbody id="tbody">
    {{#posts}}
<!--    mustache에서의 반복문(mustache : html을 만들어주는 템플릿 엔진)-->
        <tr>
            <td>{{id}}</td>
<!--            td : 하나씩 데이터 입력. table data? 인듯-->
            <td><a href="/posts/update/{{id}}">{{title}}</a> </td>
<!--            p11-1 posts/update/... 게시글 수정 화면으로 이동-->
            <td>{{author}}</td>
            <td><a href="/posts/update/{{id}}" class="btn btn-primary" role="button">수정</a></td>
        </tr>
    {{/posts}}
    </tbody>
</table>
</div>
{{>layout/footer}}